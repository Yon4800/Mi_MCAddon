{
  "format_version": "1.10.0",
  "animation_controllers": {
    "controller.animation.yosano.move": {
      "initial_state": "default",
      "states": {
        "default": {
          "animations": [
            "humanoid_base_pose",
            "look_at_target_default"
          ],
          "transitions": [
            { "moving": "query.modified_move_speed > 0.02 || query.is_moving" }
          ]
        },
        "moving": {
          "animations": [
            "humanoid_base_pose",
            "move",
            "look_at_target_default"
          ],
          "transitions": [
            { "default": "query.modified_move_speed <= 0.02 && !query.is_moving" }
          ]
        }
      }
    }
  }
}
